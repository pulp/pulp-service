Added RBAC access policy definitions to pulp-service viewsets (VulnerabilityReport, PyPIYankMonitor,
FeatureContentGuard, TaskViewSet, CreateDomainView, MigrateDomainView) using pulpcore's standard
AccessPolicy framework (infrastructure only, not yet enforced).
