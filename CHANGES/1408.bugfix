Fixed the Lightwell scheduled sync creating content in the wrong domain. The task dispatched ``pulp_python``'s sync while the default domain was active, so the synced content units and artifacts were stamped with, and stored in, the default domain instead of the repository's. The sync is now dispatched within the repository's domain using ``with_domain``, so content and artifacts land in the correct domain's storage.
