From 53bd1fda243980e6522646e520217cc5ae0cd0da Mon Sep 17 00:00:00 2001
From: git-hyagi <45576767+git-hyagi@users.noreply.github.com>
Date: Wed, 26 Feb 2025 10:20:52 -0300
Subject: [PATCH] Rollback test_download_policies pydantic changes

---
 .../functional/api/test_download_policies.py  | 20 +++++++++++++------
 1 file changed, 14 insertions(+), 6 deletions(-)

diff --git a/pulp_rpm/tests/functional/api/test_download_policies.py b/pulp_rpm/tests/functional/api/test_download_policies.py
index 287ad452..6723c5d4 100644
--- a/pulp_rpm/tests/functional/api/test_download_policies.py
+++ b/pulp_rpm/tests/functional/api/test_download_policies.py
@@ -17,7 +17,7 @@ def test_download_policies(
     rpm_publication_api,
     gen_object_with_cleanup,
     delete_orphans_pre,
-    get_content_summary,
+    #get_content_summary,
 ):
     """Sync repositories with the different ``download_policy``.
 
@@ -40,9 +40,14 @@ def test_download_policies(
 
     # Step 3, 4
     assert repo.latest_version_href.endswith("/1/")
-    content_summary = get_content_summary(repo)
-    assert content_summary["present"] == RPM_FIXTURE_SUMMARY
-    assert content_summary["added"] == RPM_FIXTURE_SUMMARY
+    #content_summary = get_content_summary(repo)
+    #assert content_summary["present"] == RPM_FIXTURE_SUMMARY
+    #assert content_summary["added"] == RPM_FIXTURE_SUMMARY
+    repo_ver = rpm_repository_version_api.read(repo.latest_version_href)
+    present_summary = {k: {"count": v["count"]} for k, v in repo_ver.content_summary.present.items()}
+    assert present_summary == RPM_FIXTURE_SUMMARY
+    added_summary = {k: {"count": v["count"]} for k, v in repo_ver.content_summary.added.items()}
+    assert added_summary == RPM_FIXTURE_SUMMARY
 
     # Step 5
     latest_version_href = repo.latest_version_href
@@ -50,8 +55,11 @@ def test_download_policies(
 
     # Step 6, 7
     assert latest_version_href == repo.latest_version_href
-    content_summary = get_content_summary(repo)
-    assert content_summary["present"] == RPM_FIXTURE_SUMMARY
+    #content_summary = get_content_summary(repo)
+    #assert content_summary["present"] == RPM_FIXTURE_SUMMARY
+    repo_ver = rpm_repository_version_api.read(repo.latest_version_href)
+    present_summary = {k: {"count": v["count"]} for k, v in repo_ver.content_summary.present.items()}
+    assert present_summary == RPM_FIXTURE_SUMMARY
 
     # Step 8
     publish_data = RpmRpmPublication(repository=repo.pulp_href)
-- 
2.46.2

