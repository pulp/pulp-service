diff --git a/pulpcore/cache/cache.py b/pulpcore/cache/cache.py
index ab1888a80..4892eefcb 100644
--- a/pulpcore/cache/cache.py
+++ b/pulpcore/cache/cache.py
@@ -362,7 +362,7 @@ class AsyncContentCache(AsyncCache):
             else:
                 if size := response.headers.get("X-PULP-ARTIFACT-SIZE"):
                     artifacts_size_counter.add(size)
-                self._check_not_modified(request, response)
+            self._check_not_modified(request, response)
 
             if isinstance(response, HTTPException):
                 raise response
@@ -372,8 +372,16 @@ class AsyncContentCache(AsyncCache):
 
     @staticmethod
     def _check_not_modified(request, response):
-        """Raise HTTPNotModified if the cached response satisfies If-Modified-Since."""
-        last_modified = response.headers.get("Last-Modified")
+        """Raise HTTPNotModified if the response satisfies conditional request headers."""
+        headers = dict(response.headers)
+
+        etag = headers.get("ETag")
+        if etag:
+            if_none_match = request.headers.get("If-None-Match")
+            if if_none_match and etag in {v.strip() for v in if_none_match.split(",")}:
+                raise HTTPNotModified(headers=headers)
+
+        last_modified = headers.get("Last-Modified")
         if not last_modified:
             return
         ims_header = request.headers.get("If-Modified-Since")
@@ -384,8 +392,6 @@ class AsyncContentCache(AsyncCache):
             return
         lm_epoch = parse_http_date_safe(last_modified)
         if lm_epoch is not None and lm_epoch <= ims_epoch:
-            headers = dict(response.headers)
-            headers["X-PULP-CACHE"] = "HIT"
             raise HTTPNotModified(headers=headers)
 
     def get_request_from_args(self, args):
diff --git a/pulpcore/content/handler.py b/pulpcore/content/handler.py
index 7d9fca8cc..4f7eacaa0 100644
--- a/pulpcore/content/handler.py
+++ b/pulpcore/content/handler.py
@@ -1212,9 +1212,14 @@ class Handler:
         Returns:
             The [aiohttp.web.FileResponse][] for the file.
         """
-        artifact_file = content_artifact.artifact.file
+        artifact = content_artifact.artifact
+        artifact_file = artifact.file
         content_length = artifact_file.size
 
+        etag = f'"{artifact.sha256}"'
+        headers["ETag"] = etag
+        headers["Cache-Control"] = "public, max-age=0, must-revalidate"
+
         try:
             range_start, range_stop = request.http_range.start, request.http_range.stop
             if range_start or range_stop:
