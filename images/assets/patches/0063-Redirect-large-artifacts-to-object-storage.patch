diff --git a/pulpcore/content/handler.py b/pulpcore/content/handler.py
index f1cb2b5..3608d81 100644
--- a/pulpcore/content/handler.py
+++ b/pulpcore/content/handler.py
@@ -69,6 +69,8 @@ from pulpcore.metrics import artifacts_size_counter  # noqa: E402
 log = logging.getLogger(__name__)
 _current_distribution = ContextVar("current_distribution", default=None)
 
+LARGE_FILE_REDIRECT_THRESHOLD = 1_700_000_000
+
 
 class PathNotResolved(HTTPNotFound):
     """
@@ -1141,7 +1143,10 @@ class Handler:
             if not os.path.exists(path):
                 raise Exception(_("Expected path '{}' is not found").format(path))
             return FileResponse(path, headers=headers)
-        elif not domain.redirect_to_object_storage:
+        elif (
+            not domain.redirect_to_object_storage
+            and artifact_file.size <= LARGE_FILE_REDIRECT_THRESHOLD
+        ):
             return ArtifactResponse(content_artifact.artifact, headers=headers)
         elif domain.storage_class in (
             "storages.backends.s3boto3.S3Boto3Storage",
